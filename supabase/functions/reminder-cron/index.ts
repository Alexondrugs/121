import { serve } from "https://deno.land/std@0.203.0/http/server.ts";

serve(async (_req) => {
  // Placeholder: query upcoming due tasks and send Web Push & Telegram
  // Implement with Supabase client and VAPID keys in secrets
  return new Response('ok')
})


